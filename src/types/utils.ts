export enum LogLevel {
  Error = "ERROR",
  Warning = "WARNING",
  Info = "INFO",
  Debug = "DEBUG",
}

export enum ScreenType {
  Agenda = "Agenda",
  Timeline = "Timeline",
}

export enum ListenerAction {
  EventDeleted = "Event Deleted",
  EventCreated = "Event Created",
  MessageReceived = "New Message",
  MediaReceived = "New Media",
  NoAction = "No Action"
}
